import React from 'react';

// eslint-disable-next-line
import styles from './IconClose.scss';

const IconClose = ({ onClick }) => <div className="close" onClick={onClick} />;

export default IconClose;
